document.addEventListener (
  'DOMContentLoaded',
  () => {
    console.log ('IronGenerator JS imported successfully!');
  },
  false
);

$ (document).ready (function () {
  console.log ('ready!');
});

window.scroll ({
  behavior: 'smooth',
});
